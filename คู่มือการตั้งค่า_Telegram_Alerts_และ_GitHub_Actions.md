# 🤖 คู่มือการตั้งค่าระบบแจ้งเตือนอัตโนมัติเข้า Telegram ผ่าน GitHub Actions

ระบบนี้ถูกออกแบบมาเพื่อส่งการแจ้งเตือนและการวิเคราะห์ตลาดเข้าสู่ **Telegram** ของคุณโดยอัตโนมัติ โดยใช้ **GitHub Actions** เป็นตัวประมวลผลบนคลาวด์ฟรี 100% ไม่ต้องเปิดคอมพิวเตอร์ทิ้งไว้

---

## 🌟 จุดเด่นใหม่: ตั้งค่าและเปลี่ยนข้อมูลผ่านหน้าเว็บได้ทันที!
คุณสามารถกรอกหรือเปลี่ยน **Telegram Bot Token** และ **Chat ID** ผ่านหน้าเว็บ Dashboard ได้โดยตรง:
- ไปที่แท็บ **"ตั้งค่าระบบ" (Settings)** หรือคลิกปุ่ม **"📱 ตั้งค่าแจ้งเตือน Telegram"** จากหน้าวิเคราะห์ทองหรือน้ำมัน
- มีปุ่ม **"🧪 ทดสอบส่งเข้า Telegram"** เพื่อลองส่งข้อความจริงทันที
- มีปุ่ม **"🌟 ทดสอบส่งสรุปข่าวทองคำตอนนี้"** เพื่อดูตัวอย่างผลวิเคราะห์ข่าวจริงวันนี้
- เมื่อกด **"💾 บันทึกข้อมูล Telegram"** ระบบจะบันทึกลงฐานข้อมูล Cloud ทันที ทำให้ **GitHub Actions สามารถดึงข้อมูลใหม่ล่าสุดไปใช้ส่งอัตโนมัติ โดยที่คุณไม่ต้องเข้าไปแก้ GitHub Secrets อีกเลย!**

---

## 📅 ตารางการทำงานของระบบอัตโนมัติ

| รายการแจ้งเตือน | วันและเวลาที่ส่ง (เวลาไทย GMT+7) | เนื้อหาที่ส่ง | ช่องทาง/รูปแบบ |
| :--- | :--- | :--- | :--- |
| 🌟 **AI News Digest (XAU/USD)** | **จันทร์ - ศุกร์** เวลา **08:00 น.** | สรุปข่าวเศรษฐกิจที่มีผลต่อทองคำทุกข่าว เรียงลำดับเวลา เช้า-ค่ำ พร้อมระดับความสำคัญ (🔴 High / 🟡 Medium) ตัวเลขคาดการณ์/ครั้งก่อน และบทวิเคราะห์ทิศทางกระทบราคาทองคำ | ข้อความ HTML จัดรูปแบบสวยงาม |
| 🛢️ **WTI Oil Chart Screenshot** | **ทุกวันอังคาร** เวลา **08:00 น.** | รูปแคปหน้าจอส่วน **"📊 กราฟสด investing.com: 🛢️ WTI Crude Oil"** พร้อมตารางอัปเดตราคาน้ำมันขายปลีกในไทย | รูปภาพความละเอียดสูง (Retina) พร้อมคำบรรยาย |

---

## 🛠️ โครงสร้างไฟล์ในระบบ

```text
├── .github/
│   └── workflows/
│       └── telegram_alerts.yml             # เวิร์กโฟลว์ GitHub Actions (ตั้งเวลา Cron + Dispatch)
├── dashboard/
│   ├── functions/api/
│   │   ├── settings.js                     # API บันทึกและดึงการตั้งค่า Telegram บน Cloudflare D1
│   │   └── telegram.js                     # API ทดสอบส่งข้อความเข้า Telegram จากหน้าเว็บ
│   └── src/components/
│       └── TelegramSettingsCard.jsx        # คอมโพเนนต์หน้าตั้งค่าและปุ่มทดสอบ Telegram ในเว็บ
├── scripts/
│   ├── send_gold_news.py                   # ดึงข่าวเศรษฐกิจ วิเคราะห์ผลกระทบทองคำ และส่ง Telegram
│   ├── send_oil_chart.py                   # ใช้ Playwright แคปจอกราฟสด WTI และส่งรูปเข้า Telegram
│   ├── test_local.py                       # สคริปต์ทดสอบการเชื่อมต่อ Bot ในเครื่อง
│   └── requirements.txt                    # แพ็กเกจสำหรับ GitHub Actions (playwright)
└── คู่มือการตั้งค่า_Telegram_Alerts_และ_GitHub_Actions.md
```

---

## 📌 ขั้นตอนที่ 1: สร้าง Telegram Bot และหา Chat ID

### 1.1 สร้าง Bot เพื่อรับ `TELEGRAM_BOT_TOKEN`
1. เปิดแอป Telegram แล้วค้นหา **`@BotFather`** (มีติ๊กถูกสีฟ้า)
2. กดปุ่ม **Start** หรือพิมพ์คำสั่ง `/newbot`
3. ตั้งชื่อบอท เช่น: `Onicorn Trade Alert`
4. ตั้ง Username ของบอทลงท้ายด้วย `bot` เช่น: `onicorn_trade_alert_bot`
5. เมื่อสร้างเสร็จ BotFather จะให้ **HTTP API Token** มา เช่น:
   `7829103845:AAF1_xxxxxxxxx_xxxxxxxxx` (คัดลอกเก็บไว้)

### 1.2 หา `TELEGRAM_CHAT_ID` ของคุณ
* **กรณีรับการแจ้งเตือนในแชทส่วนตัว:**
  1. กดค้นหาบอทของคุณที่เพิ่งสร้างขึ้นมา แล้วกดปุ่ม **Start** 1 ครั้ง (สำคัญมาก! เพื่อเปิดการสนทนา)
  2. ค้นหาบอท **`@userinfobot`** แล้วกด Start จะเห็นรหัส **Id** ของคุณ เช่น `123456789` (คัดลอกเก็บไว้)
* **กรณีรับการแจ้งเตือนในกลุ่ม / กลุ่มช่อง (Channel/Group):**
  1. ดึงบอทของคุณเข้ากลุ่มหรือ Channel นั้น และตั้งสิทธิ์บอทเป็น **Admin** (ส่งข้อความได้)
  2. เพิ่มบอท **`@RawDataBot`** เข้ากลุ่มเพื่อดู `chat.id` (มักติดลบ เช่น `-1001234567890`) เมื่อได้แล้วเตะ `@RawDataBot` ออกได้ครับ

---

## 💻 ขั้นตอนที่ 2: กรอกข้อมูลในหน้าเว็บ Dashboard (วิธีที่ง่ายที่สุด)

1. เปิดหน้าเว็บ Dashboard ของคุณ (หรือ `https://onicorn-trade.pages.dev`)
2. ไปที่แท็บ **"ตั้งค่าระบบ" (Settings)**
3. จะพบการ์ด **"📱 ตั้งค่าระบบแจ้งเตือน Telegram (Telegram Alerts Integration)"**
4. นำ **Bot Token** และ **Chat ID** มาวางในช่อง
5. กดปุ่ม **"🧪 ทดสอบส่งเข้า Telegram"** ➔ จะมีข้อความทดสอบเด้งเข้าสู่ Telegram ทันทีเพื่อยืนยันว่าเชื่อมต่อถูกต้อง
6. กดปุ่ม **"💾 บันทึกข้อมูล Telegram"** ➔ เรียบร้อย! ระบบคลาวด์จะจำค่านี้ไว้ และ GitHub Actions จะดึงไปใช้อัตโนมัติ

*(หมายเหตุ: คุณยังสามารถใส่ใน GitHub Secrets เป็นค่าสำรองได้ตามความต้องการ)*

---

## 🚀 ขั้นตอนที่ 3: Push โค้ดขึ้น GitHub

ใช้คำสั่งนี้ใน Terminal เพื่ออัปเดตไฟล์ทั้งหมดขึ้นสู่ GitHub Repository:

```bash
# 1. เพิ่มไฟล์เข้า Git
git add .

# 2. บันทึก Commit
git commit -m "feat: add web telegram settings and automated alerts"

# 3. Push ขึ้น GitHub
git branch -M main
git push -u origin main
```

---

## 🧪 ขั้นตอนที่ 4: การทดสอบการทำงานบน GitHub Actions

1. เข้าไปที่แท็บ **Actions** บน GitHub Repository ของคุณ
2. ในแถบด้านซ้ายคลิกที่ **Automated Telegram Alerts (Gold & Oil)**
3. ด้านขวาจะมีปุ่ม **Run workflow** ให้คลิก
4. เลือกตัวเลือกงานที่ต้องการทดสอบ:
   - `gold_news` = ทดสอบส่งสรุปข่าวทองคำ
   - `oil_chart` = ทดสอบแคปจอกราฟน้ำมันและส่งรูปภาพ
   - `both` = ทดสอบส่งทั้งสองรายการพร้อมกัน
5. กดปุ่มสีเขียว **Run workflow** แล้วรอประมาณ 30-60 วินาที ข้อความและรูปภาพจะเด้งเข้าสู่ Telegram ของคุณทันที!

---

## ⏰ สรุปเรื่องเวลาการทำงาน (Cron Schedule)
- เซิร์ฟเวอร์ GitHub Actions อ้างอิงเวลาสากล **UTC**
- เวลาประเทศไทยคือ **UTC+7**
- เวลา **08:00 น.** ของไทย = **01:00 น. UTC**
- รูปแบบ Cron ที่ตั้งไว้:
  - `0 1 * * 1-5` (จันทร์ - ศุกร์ เวลา 01:00 UTC = 08:00 น. ไทย)
  - ระบบจะตรวจสอบวันอัตโนมัติ: วันจันทร์-ศุกร์ส่งข่าวทองคำ และวันอังคารจะส่งทั้งข่าวทองคำและรูปกราฟน้ำมัน WTI ครับ
